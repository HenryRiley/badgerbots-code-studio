# Student Connect installer

Planned low-interaction Windows installer. It installs Connect, establishes a stable device ID, finds the managed MultiMC/Prism instance, installs/updates the profile and client mod, adds the BadgerBots server, lets an instructor map the fixed Minecraft username, and verifies readiness.
