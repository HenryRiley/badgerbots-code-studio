# BadgerBots Connect

Planned lightweight Tauri 2 Windows helper for fixed student laptops. It keeps a persistent device identifier and Minecraft username mapping, verifies MultiMC/Prism and the managed profile, installs/updates the required client mod, adds the server, reports local connection state, and associates the weekly camper login with the device.

The device mapping persists across camps. Camper data does not.
