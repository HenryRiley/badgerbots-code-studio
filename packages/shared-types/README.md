# Shared types

Cross-application opaque identifiers, instructor roles, retention states, revision conflicts, program authors, and compact realtime hints. Provider SDK types stay out of this package so applications and later generated clients share a portable contract.
